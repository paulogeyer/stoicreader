<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to 'StoicReader', root_path, class: 'navbar-brand' %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
	<%= link_to 'Timeline', feeds_path, class: 'nav-link' %>
      </li>
      <li class="nav-item">
	<%= link_to 'Subscriptions', subscriptions_path, class: 'nav-link' %>
      </li>
    </ul>
    <ul class="navbar-nav ml-auto">
      <li class="nav-item">
	<% if current_user %>
	<%= current_user.email %>
	<%= link_to 'Logout', destroy_user_session_path, method: :delete, data: {confirm: "Are you sure?"} %>
	<% else %>
	<a href="#">Login</a>
	<% end %>
      </li>
    </ul>
    </ul>
  </div>
</nav>
